import Link from 'next/link'; import type { Book } from '@/lib/types';
export function BookCard({book,action}:{book:Book;action?:React.ReactNode}){return <article className="book-card"><div className="cover" aria-hidden="true">BOOK</div><div><p className="eyebrow">{book.category||'Library book'}</p><h3>{book.title}</h3><p>{book.author}</p><span className={book.available_copies?'status available':'status unavailable'}>{book.available_copies?`${book.available_copies} copies available`:'Out of stock'}</span><div className="book-actions"><Link className="text-button" href={`/books/${book.id}`}>Details</Link>{action}</div></div></article>}
