'use client';
import { useEffect,useState } from 'react'; import { AppShell } from '@/components/app-shell'; import { Guard } from '@/components/guard'; import { api } from '@/lib/api'; import { sampleBooks } from '@/lib/sample-data'; import type { Book } from '@/lib/types';
export default function MyBooks(){const [books,setBooks]=useState<Book[]>(sampleBooks),[message,setMessage]=useState('');useEffect(()=>{api.books().then(setBooks).catch(()=>setMessage('Unable to refresh books; displaying saved data.'))},[]);return <Guard><AppShell><h1>My books</h1><p className="lead">Renew eligible books before their due date.</p>{message&&<p className="notice">{message}</p>}<div className="table-wrap"><table><thead><tr><th>Book</th><th>Due date</th><th>Renewals</th><th>Fine</th><th></th></tr></thead><tbody>{books.map(b=><tr key={b.id}><td><b>{b.title}</b><br/><small>{b.author}</small></td><td>{b.due_date||'—'}</td><td>{b.renewal_count||0}</td><td>₹{b.fine_amount||0}</td><td><button className="small" onClick={async()=>{try{await api.extend(b.id);setMessage('Extension requested successfully.')}catch(e){setMessage(e instanceof Error?e.message:'Unable to extend.')}}}>Extend</button></td></tr>)}</tbody></table></div></AppShell></Guard>}
