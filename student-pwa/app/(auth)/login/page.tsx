'use client';
import Link from 'next/link'; import { useRouter } from 'next/navigation'; import { FormEvent, useState } from 'react'; import { api } from '@/lib/api';
export default function Login(){const r=useRouter(),[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),student_id=String(f.get('student_id')),password=String(f.get('password'));if(!/^\d{3}\/\d{2}$/.test(student_id)){setError('Use the format 001/26.');return}setBusy(true);try{const out=await api.login(student_id,password);localStorage.setItem('library_token',out.token);r.push('/')}catch(e){setError(e instanceof Error?e.message:'Login failed.')}finally{setBusy(false)}}return <main className="auth"><form className="auth-card" onSubmit={submit}><p className="eyebrow">SMART LIBRARY</p><h1>Welcome back</h1><p>Sign in to manage your reading.</p><label>Student ID<input name="student_id" placeholder="001/26" required /></label><label>Password<input name="password" type="password" required /></label><label className="check"><input type="checkbox" name="remember" defaultChecked /> Keep me signed in</label>{error&&<p className="error" role="alert">{error}</p>}<button disabled={busy}>{busy?'Signing in…':'Sign in'}</button><p>New here? <Link href="/register">Create your account</Link></p></form></main>}
