'use client';
import Link from 'next/link'; import { useRouter } from 'next/navigation'; import { FormEvent, useState } from 'react'; import { api } from '@/lib/api';
export default function Register(){const r=useRouter(),[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const form=new FormData(e.currentTarget),data=Object.fromEntries(form) as Record<string,string>;if(!/^\d{3}\/\d{2}$/.test(data.student_id)){setError('Student ID must be in the format 001/26.');return}setBusy(true);try{const out=await api.register(data);localStorage.setItem('library_token',out.token);r.push('/')}catch(e){setError(e instanceof Error?e.message:'Registration failed.')}finally{setBusy(false)}}return <main className="auth"><form className="auth-card" onSubmit={submit}><p className="eyebrow">SMART LIBRARY</p><h1>Create account</h1><label>Name<input name="name" required /></label><label>Student ID<input name="student_id" placeholder="001/26" required /></label><label>Email<input name="email" type="email" required /></label><label>Password<input name="password" type="password" minLength={8} required /></label>{error&&<p className="error">{error}</p>}<button disabled={busy}>{busy?'Creating…':'Create account'}</button><p>Already registered? <Link href="/login">Sign in</Link></p></form></main>}
