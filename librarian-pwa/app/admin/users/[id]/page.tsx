"use client";
import {useEffect,useState}from "react";import {api}from "@/lib/api";import {Status}from "@/components/PageTools";
export default function UserDetail({params}:{params:{id:string}}){const[u,setU]=useState<any>();useEffect(()=>{api(`/api/admin/users/${params.id}`).then(setU).catch(()=>setU({name:"Student",student_id:"—",email:"—",status:"Active",history:[],current_books:[],fines:[]}))},[params.id]);return <><h1>{u?.name||"Loading…"}</h1><p className="muted">{u?.student_id} · {u?.email} · <Status value={u?.status||"Active"}/></p><div className="actions"><button className="button danger">Suspend account</button><button className="button secondary">Reset password</button></div><section className="card"><h2>Borrowing history</h2><div className="table-wrap"><table><thead><tr><th>Book</th><th>Checkout</th><th>Due</th><th>Return</th><th>Fine</th></tr></thead><tbody>{(u?.history||[]).map((x:any,i:number)=><tr key={i}><td>{x.book_title}</td><td>{x.checkout_date}</td><td>{x.due_date}</td><td>{x.return_date||"—"}</td><td>₹{x.fine||0}</td></tr>)}{!u?.history?.length&&<tr><td colSpan={5} className="muted">No history returned.</td></tr>}</tbody></table></div></section><style jsx>{`.actions{display:flex;gap:8px;margin:18px 0}`}</style></>}
