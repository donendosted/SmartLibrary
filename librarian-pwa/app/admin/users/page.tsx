"use client";
import {useEffect,useMemo,useState}from "react";import Link from "next/link";import {api,unwrap}from "@/lib/api";import {Search,Status}from "@/components/PageTools";type User={id:number;student_id:string;name:string;email:string;books_borrowed?:number;outstanding_fines?:number;status?:string};
export default function Users(){const [users,setUsers]=useState<User[]>([]),[q,setQ]=useState("");useEffect(()=>{api<unknown>("/api/admin/users?limit=100").then(r=>setUsers(unwrap(r as {data:User[]})||[])).catch(()=>{})},[]);const rows=useMemo(()=>users.filter(u=>`${u.name} ${u.student_id}`.toLowerCase().includes(q.toLowerCase())),[q,users]);return <><h1>Students</h1><p className="muted">Student accounts, loans and fine balances.</p><section className="card"><Search placeholder="Search by name or student ID" onChange={setQ}/><div className="table-wrap"><table><thead><tr><th>Student ID</th><th>Name</th><th>Email</th><th>Borrowed</th><th>Fines</th><th>Status</th><th/></tr></thead><tbody>{rows.map(u=><tr key={u.id}><td>{u.student_id}</td><td>{u.name}</td><td>{u.email}</td><td>{u.books_borrowed??0}</td><td>₹{u.outstanding_fines??0}</td><td><Status value={u.status||"Active"}/></td><td><Link className="button secondary" href={`/admin/users/${u.id}`}>View</Link></td></tr>)}{!rows.length&&<tr><td colSpan={7} className="muted">No students found.</td></tr>}</tbody></table></div></section></>}
