"use client";
import {useEffect,useState}from "react";import {api,unwrap}from "@/lib/api";import {Status}from "@/components/PageTools";type Hold={id:number;book_title:string;student_id:string;created_at:string;position:number;status:string};
export default function Holds(){const[holds,setHolds]=useState<Hold[]>([]);useEffect(()=>{api<unknown>("/api/admin/holds?limit=100").then(r=>setHolds(unwrap(r as {data:Hold[]})||[])).catch(()=>{})},[]);return <><h1>Holds & reservations</h1><p className="muted">Manage waiting lists and ready-for-collection notices.</p><section className="card"><div className="table-wrap"><table><thead><tr><th>Book</th><th>Student</th><th>Requested</th><th>Queue</th><th>Status</th><th/></tr></thead><tbody>{holds.map(h=><tr key={h.id}><td>{h.book_title}</td><td>{h.student_id}</td><td>{h.created_at?.slice(0,10)}</td><td>{h.position}</td><td><Status value={h.status}/></td><td><button className="button secondary">Notify</button></td></tr>)}{!holds.length&&<tr><td colSpan={6} className="muted">No active holds found.</td></tr>}</tbody></table></div></section></>}
