"use client";
import {useEffect,useState}from "react";import {api,unwrap}from "@/lib/api";
export default function Reports(){const[overdue,setOverdue]=useState<any[]>([]);useEffect(()=>{api<unknown>("/api/admin/reports/overdue").then(r=>setOverdue(unwrap(r as {data:any[]})||[])).catch(()=>{})},[]);return <><header className="head"><div><h1>Reports</h1><p className="muted">Usage and circulation summaries.</p></div><button className="button secondary" onClick={()=>window.print()}>Print / save PDF</button></header><div className="metrics">{[["Most borrowed","See transaction history"],["Fine collection","Available from backend reporting"],["Engagement","Active student activity"]].map(([a,b])=><article className="card" key={a}><h2>{a}</h2><p className="muted">{b}</p></article>)}</div><section className="card"><h2>Overdue books</h2><div className="table-wrap"><table><thead><tr><th>Student</th><th>Book</th><th>Due date</th><th>Fine</th></tr></thead><tbody>{overdue.map((x,i)=><tr key={i}><td>{x.student_id||x.student_name}</td><td>{x.book_title}</td><td>{x.due_date}</td><td>₹{x.fine||0}</td></tr>)}{!overdue.length&&<tr><td colSpan={4} className="muted">No overdue books reported.</td></tr>}</tbody></table></div></section><style jsx>{`.head{display:flex;justify-content:space-between;align-items:start;margin-bottom:24px}.metrics{display:grid;grid-template-columns:repeat(3,1fr);gap:16px;margin-bottom:16px}@media(max-width:700px){.metrics{grid-template-columns:1fr}}`}</style></>}
