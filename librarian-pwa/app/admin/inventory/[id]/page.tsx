"use client";
import {useEffect,useState}from "react";import {api}from "@/lib/api";import {Status}from "@/components/PageTools";
export default function BookDetails({params}:{params:{id:string}}){const[book,setBook]=useState<any>(null);useEffect(()=>{api(`/api/books/${params.id}`).then(setBook).catch(()=>setBook({title:"Book details",isbn:"—",author:"—",category:"—",copies:[]}))},[params.id]);return <><h1>{book?.title||"Loading…"}</h1><p className="muted">ISBN {book?.isbn||"—"} · {book?.author||"—"} · {book?.category||"Uncategorised"}</p><section className="card"><h2>Individual copies</h2><div className="table-wrap"><table><thead><tr><th>Copy ID</th><th>Barcode</th><th>Location</th><th>Status</th><th>Action</th></tr></thead><tbody>{(book?.copies||[]).map((c:any)=><tr key={c.id}><td>{c.id}</td><td>{c.barcode}</td><td>{c.location||"Main library"}</td><td><Status value={c.status}/></td><td><button className="button secondary">Update</button></td></tr>)}{!book?.copies?.length&&<tr><td colSpan={5} className="muted">No individual copy records returned by the API.</td></tr>}</tbody></table></div></section></>}
