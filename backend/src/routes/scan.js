import { Router } from 'express'; import { withTransaction } from '../db.js'; import { checkout,returnCopy } from '../services/library.js'; import { apiError,isStudentId } from '../utils.js'; const router=Router();
router.post('/',async(req,res)=>{const {action,barcode,student_id}=req.body;if(!barcode||!['checkout','return'].includes(action))throw apiError(400,'action (checkout|return) and barcode are required','VALIDATION_ERROR');if(action==='checkout'&&!isStudentId(student_id))throw apiError(400,'student_id must use XXX/YY format','INVALID_STUDENT_ID');const transaction=await withTransaction(c=>action==='checkout'?checkout(c,{barcode,student_id}):returnCopy(c,{barcode}));res.json({action,transaction});}); export default router;
