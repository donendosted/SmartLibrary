# MongoDB setup

No SQL schema is required. The backend creates MongoDB collections and indexes automatically when it connects using `MONGODB_URI`.

The former PostgreSQL schema was intentionally removed as part of the MongoDB migration.
